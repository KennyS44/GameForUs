# A zip of this very repository, built so the game can be uploaded to other
# static hosts without a terminal. It is a derived artefact — rebuild it rather
# than keeping a copy of the repo inside the repo.
gameforus.zip

# Editor and OS noise
.DS_Store
Thumbs.db
*.swp

# Screenshots taken by tools/shot.mjs — regenerate them, do not keep them.
docs/shots/
